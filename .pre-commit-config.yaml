# Pre-commit hooks configuration for kvs-conversion-server
# See https://pre-commit.com for more information

# Define the minimum version
minimum_pre_commit_version: '3.5.0'

# Don't run on tags
fail_fast: false

# Default settings
default_language_version:
  python: python3.13

default_install_hook_types:
  - pre-commit
  - commit-msg

repos:
  # ShellCheck - Static analysis tool for shell scripts
  - repo: https://github.com/koalaman/shellcheck-precommit
    rev: v0.11.0
    hooks:
      - id: shellcheck
        name: "ShellCheck - Static analysis for shell scripts"
        args: ["--severity=warning"]  # Show errors and warnings
        types: [shell]
        files: \.(sh|bash|ksh|zsh)$

  # Bashate - Code style checker for bash scripts
  - repo: https://github.com/openstack/bashate
    rev: 2.1.1
    hooks:
      - id: bashate
        name: "Bashate - Bash code style enforcement"
        args: ["-i", "E006,E005,E003"]  # Ignore line length and indent style (we use 2 spaces, not 4)
        files: \.(sh|bash)$

  # General file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-added-large-files
        name: "Check for large files"
        args: ['--maxkb=1000']
      - id: check-executables-have-shebangs
        name: "Check executable shebangs"
      - id: check-merge-conflict
        name: "Check for merge conflicts"
      - id: check-symlinks
        name: "Check symlinks"
      - id: end-of-file-fixer
        name: "Fix end of file"
      - id: trailing-whitespace
        name: "Remove trailing whitespace"

  # Commitizen - Conventional commits
  - repo: https://github.com/commitizen-tools/commitizen
    rev: v4.9.1
    hooks:
      - id: commitizen
        name: "Check commit message format"
        stages: [commit-msg]
      - id: commitizen-branch
        name: "Check branch commits"
        stages: [pre-push]
